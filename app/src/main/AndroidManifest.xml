<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moviesdirector">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviesDirector">
        <activity
            android:name="com.example.moviesdirector.presenter.view.AboutScreenActivity"
            android:exported="false" />
        <activity
            android:name="com.example.moviesdirector.presenter.view.DetailsMoviesActivity"
            android:exported="false" />
        <activity
            android:name="com.example.moviesdirector.presenter.view.ListMoviesActivity"
            android:exported="false" />
        <activity
            android:name="com.example.moviesdirector.presenter.view.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>